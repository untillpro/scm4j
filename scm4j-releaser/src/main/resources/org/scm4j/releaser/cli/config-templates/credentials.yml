# This yaml file contains credentials for vcs repositories used by components
# Credentials of the first regex entry which matches the repository url will be used. 
# If no matches then `~` credentials will be used 
# 
#- https://my.company/internal.(.*):
#  name: internalRepoUser
#  password: internalPass
#- https://my.company.(.*):
#  name: publicRepoUser
#- https://(.*) # one password for all urls which has no password specified above and started with https://
#  password: commonPass
#- ~: # used if url matches none of above
#  name: defaultUser
#  password: defaultPass
